name: TradingView Scraper

on:
  schedule:
    # Runs at 1:00 AM UTC every day (change as needed)
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        run: |
          pip install --upgrade pip
          pip install selenium beautifulsoup4 gspread webdriver-manager oauth2client

      - name: 🔑 Set up Google Credentials
        run: echo "${{ secrets.GSPREAD_CREDENTIALS }}" > credentials.json

      - name: 🍪 Set up TradingView Cookies
        run: echo "${{ secrets.TRADINGVIEW_COOKIES }}" > cookies.json

      - name: ⚙️ Create Checkpoint File (if missing)
        run: |
          if [ ! -f checkpoint_new_1.txt ]; then
            echo "1" > checkpoint_new_1.txt
          fi

      - name: 🚀 Run Scraper Script
        run: python run_scraper.py
