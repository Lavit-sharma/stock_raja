name: YouTube Transcript Bot

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'YouTube Video URL'
        required: true

jobs:
  run-transcript:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Libraries
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir mysql-connector-python youtube-transcript-api==1.2.4

      - name: Create Cookies File
        # Use a heredoc to ensure the content is written exactly as it appears in the secret
        run: |
          cat <<EOF > cookies.txt
          ${{ secrets.YOUTUBE_COOKIES }}
          EOF
          # Check if the file was actually created and has size
          ls -l cookies.txt

      - name: Run Transcript Bot
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
        run: |
          python transcript_downloader.py "${{ github.event.inputs.video_url }}"
